<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>URL Shortener</h1>
    <% if (locals.id) { %>
        <p>Your short URL is: <a href="http://localhost:8000/url/<%= id %>">http://localhost:8000/url/<%= id %></a> </p>
        <% } %>
            <form action="/url" method="POST">
                <label for="Enter your url">Enter your url</label>
                <input type="text" name="longUrl" id="Enter your url" placeholder="Enter your url">
                <input type="submit" value="Generate Short URL">
            </form>

            <% if (locals?.urls) { %>
                <h2>Analytics for : <% if (locals?.user) { %>
                 <%= user %>
                <% } %></h2>

                <table>
                    <tr>
                        <th>No</th>
                        <th>Short URL</th>
                        <th>Original URL</th>
                        <th>Visit Count</th>
                    </tr>
                    <% urls?.forEach((url, index)=> { %>
                        <tr>
                            <td>
                                <%= index + 1 %>
                            </td>
                            <td>
                                <%= url.shortId %>
                            </td>
                            <td>
                                <%= url.redirectUrl %>
                            </td>
                            <td>
                                <%= url.visitHistory.length %>
                            </td>
                        </tr>
                        <% }) %>
                </table>
                <% } %>
</body>

</html>